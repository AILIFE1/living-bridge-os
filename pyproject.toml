[project]
name = "living-bridge-os"
version = "0.1.0"
description = "Continuity-first multi-agent operating environment"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "sqlalchemy",
    "streamlit",
    "python-dotenv",
]